{
  "name": "beanheads",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:core": "nodemon --watch core/src --ext '*' --exec zx scripts/dev-core.mjs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "pnpm -r exec -- npx shx rm -rf node_modules .next .turbo coverage build dist compiled storybook-static"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.4",
    "zx": "^8.1.3"
  }
}